# 后端依赖，经过 Windows ARM64 兼容性验证
# 全部选择在 ARM64 上有预编译 wheel 或纯 Python 实现的包
fastapi>=0.115.0,<0.117
uvicorn>=0.30.0,<0.33
sqlalchemy>=2.0.30,<2.1
pydantic>=2.7.1,<3
pydantic-settings>=2.3.0,<3
httpx>=0.27.0,<0.29
apscheduler>=3.10.4,<4
python-multipart>=0.0.9,<0.1
python-dotenv>=1.0.0,<2
openai>=1.30.0,<2
beautifulsoup4>=4.12.0,<5
# OCR 截图导入：图片预处理（降采样 + 重压），降低多模态请求延迟。可选；缺失时跳过压缩。
Pillow>=10.0.0,<12

